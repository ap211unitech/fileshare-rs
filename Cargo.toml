[package]
name = "fileshare-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8.3"
tokio = { version = "1.44.2", features = ['full'] }

# environment variables
dotenvy = "0.15.7"

# handle json
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
